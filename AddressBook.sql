create database AddressBook_DB;
use AddressBook_DB;

CREATE TABLE AddressBook
(
 FirstName varchar(50),
 LastName varchar(50),
 Address varchar (250),
 City varchar (100),
 state varchar (100),
 ZipCode float,
 PhoneNumber float,
 EmailID varchar(100)
);

